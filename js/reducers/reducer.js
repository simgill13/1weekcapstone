import {
 
} from '../actions/action';

const initialState = {
    
    loading: false    
    
};

 const appReducer = (state = initialState, action) => {
   

    return state;
};

export default appReducer;